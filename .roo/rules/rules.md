before declaring a problem is fixed you must run all tests. 
if you adding a file there must be tests that test that file. 
if you find a problem that a test did not you must add a test for that problem. test can be generic or specific. 
